[project]
name = "whisper-transcription"
version = "2.0.0"
requires-python = ">=3.10"
dependencies = [
    "faster-whisper>=1.0.0",
    "fastapi",
    "uvicorn[standard]",
    "python-multipart",
    "aiofiles",
]
